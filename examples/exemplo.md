# Exemplo de Markdown

Este arquivo serve para testar o visualizador com um estilo inspirado no preview do VS Code.

## Recursos

- Titulos
- Listas
- Links como [OpenAI](https://openai.com)
- Codigo inline como `Console.WriteLine()`

```csharp
public static void Main()
{
    Console.WriteLine("Markdown Viewer");
}
```

```sql
SELECT id, nome, email
FROM usuarios
WHERE ativo = 1
ORDER BY nome;
```

```html
<section class="card">
    <h1>Titulo</h1>
    <p>Texto em HTML.</p>
</section>
```

```env
# Configuracao da aplicacao
APP_ENV=production
APP_DEBUG=false
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=sistemadm8
MAIL_FROM_NAME="Sistema DM8"
```

| Coluna | Descricao |
| --- | --- |
| Tabela | Renderizada com bordas |
| Tema | Claro e escuro |

> Blocos de citacao tambem aparecem com a lateral destacada.
